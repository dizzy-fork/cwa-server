---
spring:
  test:
    database:
      replace: none
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: false
  datasource:
    url: jdbc:h2:mem:cwa;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
    driverClassName: org.h2.Driver
    platform: h2
  main:
    banner-mode: off
logging:
  level:
    org:
      springframework: off
    root: off
